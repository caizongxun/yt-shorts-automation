# 手動內容工作流程 📝

**簡化版本：你提供故事 → 系統自動製作影片**

不需要 Reddit API、不需要 LLM，只需要你的創意故事！

---

## 工作流程

```
┌─────────────────────────────────────┐
│ 1. 你生成/寫故事 (或請我生成)        │
│    → 存成 TXT 檔                     │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 2. 貼到 content/ 目錄               │
│    - story_1.txt                    │
│    - story_2.txt                    │
│    - story_3.txt                    │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 3. 執行命令:                        │
│    python scripts/manual_daily_     │
│    pipeline.py --count 3            │
└──────────────┬──────────────────────┘
               │
               ▼
         ┌─────────────┐
         │  自動流程   │
         ├─────────────┤
         │ 文字→語音   │  Edge-TTS (免費)
         │ 語音→視頻   │  MoviePy + FFmpeg
         │ 視頻優化    │  隨機效果躲避演算法
         └─────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 4. 完成！                           │
│    output/videos/                   │
│    ├─ manual_short_00.mp4           │
│    ├─ manual_short_01.mp4           │
│    └─ manual_short_02.mp4           │
└─────────────────────────────────────┘
```

---

## 快速開始

### 步驟 1: 建立故事檔案

在 `content/` 目錄建立 TXT 檔案。格式：

```
Title: Your Story Title Here

Your story content goes here.
Can be multiple paragraphs.
Will be read as-is for TTS.
```

**範例檔案**: `content/story_1.txt`

```
Title: The Mystery of Why Cats Purr

Did you know cats purr for more than just happiness? When your cat purrs, 
it's actually vibrating at a frequency of 25 to 150 Hertz. Scientists believe 
this vibration can promote bone healing and muscle development. Mother cats also 
purr to guide newborn kittens, and cats may purr when they're injured or in pain 
as a self-healing mechanism. So every time your cat purrs, it might be working on 
becoming a healthier, stronger cat!
```

### 步驟 2: 準備 3 個故事

建立以下檔案在 `content/` 目錄：
- `story_1.txt`
- `story_2.txt`  
- `story_3.txt`

### 步驟 3: 執行管道

```bash
python scripts/manual_daily_pipeline.py --count 3
```

### 步驟 4: 取得影片

完成後在 `output/videos/` 找到：
- `manual_short_00.mp4`
- `manual_short_01.mp4`
- `manual_short_02.mp4`

---

## 建立範例檔案

快速建立 3 個範例故事：

```bash
python scripts/manual_daily_pipeline.py --create-examples
```

這會在 `content/` 建立：
- `example_story_1.txt` - 蜂蜜的故事
- `example_story_2.txt` - 紅鶴的故事
- `example_story_3.txt` - 細胞的故事

你可以編輯這些檔案或用它們作為範本。

---

## 進階選項

### 改變聲音

```bash
# 女性聲音
python scripts/manual_daily_pipeline.py --count 3 --voice en-female

# 可用的聲音
# en-male (預設)
# en-female
# en-casual
# en-male-old
```

### 禁用隨機效果

```bash
python scripts/manual_daily_pipeline.py --no-randomize
```

### 啟用 YouTube 上傳

```bash
python scripts/manual_daily_pipeline.py --upload
```

*注意：需要先進行 Google 帳戶授權*

---

## 排程每天自動執行

### Windows (工作排程器)

1. 按 `Win + R`，輸入 `taskschd.msc`
2. 右鍵 → 建立工作
3. 觸發程序：每天 03:00 AM
4. 動作 → 新增
   - 程式：`C:\Python311\python.exe`
   - 引數：`C:\path\to\scripts\manual_daily_pipeline.py --count 3`
   - 開始位置：`C:\path\to\yt-shorts-automation`

### Linux / Mac (Cron)

```bash
# 每天凌晨 3 點執行
0 3 * * * cd /path/to/yt-shorts-automation && python scripts/manual_daily_pipeline.py --count 3
```

編輯 crontab：
```bash
crontab -e
```

---

## 目錄結構

```
yt-shorts-automation/
├── content/                    # 你的故事檔案放這裡
│   ├── story_1.txt
│   ├── story_2.txt
│   ├── story_3.txt
│   └── processed/              # 已處理的檔案 (自動)
│
├── assets/                     # 背景素材
│   ├── gameplay/               # Minecraft 等背景影片
│   └── music/                  # 背景音樂 (可選)
│
├── output/                     # 輸出目錄
│   ├── videos/                 # 完成的影片 📹
│   ├── audio/                  # 生成的語音
│   └── metadata/               # 影片元資料
│
└── scripts/
    ├── manual_daily_pipeline.py    # 主程式 ⭐
    ├── manual_content_provider.py  # 讀取故事
    ├── audio_generator.py          # 文字→語音
    ├── video_compositor.py         # 視頻合成
    └── youtube_uploader.py         # YouTube 上傳 (可選)
```

---

## 常見問題

### Q: 故事應該有多長？

**A:** 理想長度 **200-400 字**。
- 太短 (< 100 字)：影片會太快結束
- 太長 (> 600 字)：超過 60 秒時限，系統會截斷

### Q: 可以用中文故事嗎？

**A:** 可以！但需要改變語言設定。目前預設是英文。
支援語言修改（需要修改 `audio_generator.py`）。

### Q: 影片看起來一樣怎麼辦？

**A:** 系統會自動隨機化：
- 背景影片隨機選擇和起點
- 字幕顏色隨機變化
- 色調微調 (-5% 到 +5%)
- 視覺效果隨機應用

這樣可以躲避 YouTube 演算法的重複內容檢測。

### Q: 能手動上傳而不是自動上傳嗎？

**A:** 完全可以！預設就是生成影片不自動上傳。

影片保存在 `output/videos/` 後，
你可以手動上傳或使用 YouTube Studio。

### Q: 不想用 Minecraft 背景怎麼辦？

**A:** 放你自己的背景影片到 `assets/gameplay/`：
1. 任何 MP4 影片都可以
2. 建議 1080p 或以上
3. 時長不限（系統會自動截取）

---

## 成本分析

| 項目 | 成本 |
|------|------|
| Python + 套件 | $0 |
| Edge-TTS (微軟免費服務) | $0 |
| MoviePy + FFmpeg | $0 |
| YouTube 平台 | $0 |
| 背景素材 | $0 |
| **總成本** | **$0** |

唯一可選成本是 GPU 電費（用於加速，非必需）。

---

## 工作流程示意

### 標準每日流程

```
Day 1 晚上
  └─ 你寫 3 個故事 (30 分鐘)
     └─ 貼到 content/ (2 分鐘)

Day 2 凌晨 3 點 (自動執行)
  └─ 系統生成 3 部影片 (5-10 分鐘)

Day 2 早上
  └─ 你手動上傳或設定排程 (5 分鐘)
  └─ 或啟用自動上傳
```

### 全自動模式 (需要一次性配置)

```
Day 1
  └─ 配置 Google OAuth (10 分鐘)
  └─ 設定 Cron / 工作排程 (5 分鐘)

Day 2+ (完全自動)
  └─ 將故事檔案放到 content/
  └─ 完全不需要操作！
  └─ 影片自動生成和上傳
```

---

## 下一步

✅ 準備好 3 個故事檔案

✅ 執行 `python scripts/manual_daily_pipeline.py --count 3`

✅ 檢查 `output/videos/` 中的結果

✅ (可選) 設定每日排程

✅ (可選) 啟用 YouTube 自動上傳

---

**祝你成功！** 🎬

有任何問題？檢查日誌：`logs/` 目錄有詳細的執行記錄。
